{
  "name": "ronald-filmmaker-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Server-side video processing (compression, thumbnails, cleanup) for the Ronald Filmmaker admin panel — see README.md in this folder.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,storage",
    "lint": "tsc --noEmit",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2"
  }
}
